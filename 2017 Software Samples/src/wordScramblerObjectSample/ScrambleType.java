package wordScramblerObjectSample;

//this is a simple example of an enum - notice that it is used in the Scrambler class in a switch (case...break).
public enum ScrambleType {
	Random,
	ShiftCode,
	Decode;
}
